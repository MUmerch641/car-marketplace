import Link from "next/link";
import { CarCard } from "@/components/cars/car-card";
import { SearchControls } from "@/components/cars/search-controls";
import { PageHero } from "@/components/shared/page-hero";
import { browseCars } from "@/lib/marketplace/cars";
export default async function CarsPage({ searchParams }: PageProps<"/cars">) { const params = await searchParams; const result = await browseCars(params); const values = Object.fromEntries(Object.entries(params).filter(([, value]) => typeof value === "string")) as Record<string, string>; const totalPages = Math.max(1, Math.ceil(result.count / result.perPage)); const url = (page: number) => { const query = new URLSearchParams(values); query.set("page", String(page)); return `/cars?${query}`; }; return <><PageHero eyebrow="Used cars" title="Find the right car for your road ahead." copy="Browse active listings from sellers across the UK." /><div className="mx-auto max-w-7xl px-5 py-10 lg:px-8"><SearchControls values={values} /><form action="/cars" className="mt-5 grid gap-3 border-t border-[#E4E7EC] pt-5 sm:grid-cols-4">{Object.entries(values).filter(([key]) => !["make", "postcode", "minPrice", "maxPrice", "page"].includes(key)).map(([key, value]) => <input key={key} type="hidden" name={key} value={value} />)}<select name="fuel" defaultValue={values.fuel} className="rounded-md border border-[#D0D5DD] px-3 py-2 text-sm"><option value="">Fuel type</option><option value="petrol">Petrol</option><option value="diesel">Diesel</option><option value="hybrid">Hybrid</option><option value="electric">Electric</option></select><select name="transmission" defaultValue={values.transmission} className="rounded-md border border-[#D0D5DD] px-3 py-2 text-sm"><option value="">Transmission</option><option value="manual">Manual</option><option value="automatic">Automatic</option></select><input name="maxMileage" defaultValue={values.maxMileage} placeholder="Maximum mileage" className="rounded-md border border-[#D0D5DD] px-3 py-2 text-sm" /><select name="sort" defaultValue={values.sort} className="rounded-md border border-[#D0D5DD] px-3 py-2 text-sm"><option value="newest">Newest</option><option value="price_asc">Price low to high</option><option value="price_desc">Price high to low</option><option value="mileage">Lowest mileage</option></select><button className="justify-self-start text-sm font-bold text-brand">Apply filters</button></form><div className="mt-8 flex items-center justify-between"><p className="text-sm font-semibold text-[#667085]">{result.count} {result.count === 1 ? "car" : "cars"} found</p></div>{result.cars.length ? <div className="mt-5 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{result.cars.map((car) => <CarCard key={car.id} car={car} />)}</div> : <div className="mt-5 border border-dashed border-[#D0D5DD] p-10 text-center"><h2 className="text-xl font-bold">No cars found</h2><p className="mt-2 text-[#667085]">Try removing a filter or broadening your search.</p><Link href="/cars" className="mt-4 inline-block font-bold text-brand">Clear search</Link></div>}{totalPages > 1 && <nav className="mt-8 flex items-center justify-center gap-4" aria-label="Pagination">{result.page > 1 && <Link href={url(result.page - 1)} className="font-bold text-brand">Previous</Link>}<span className="text-sm text-[#667085]">Page {result.page} of {totalPages}</span>{result.page < totalPages && <Link href={url(result.page + 1)} className="font-bold text-brand">Next</Link>}</nav>}</div></>; }
