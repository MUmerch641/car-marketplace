import { PageHero } from "@/components/shared/page-hero";
import { ServiceCard } from "@/components/services/service-card";
import { getActiveServices } from "@/lib/services/services";
export default async function ServicesPage() { const services = await getActiveServices(); return <><PageHero eyebrow="Mobile car services" title="We come to you." copy="Book practical car care where your car is parked. There is no garage visit required." /><section className="mx-auto max-w-7xl px-5 py-12 lg:px-8">{services.length ? <div className="grid gap-5 sm:grid-cols-2 lg:grid-cols-4">{services.map((service) => <ServiceCard key={service.slug} service={service} />)}</div> : <div className="border border-dashed border-[#D0D5DD] p-10 text-center"><h2 className="text-xl font-bold">No mobile services are available yet</h2><p className="mt-2 text-[#667085]">Please check back soon.</p></div>}<div className="mt-14 grid gap-5 border border-[#E4E7EC] bg-[#F5F6F7] p-7 sm:grid-cols-3"><div><h2 className="text-xl font-bold">Why book at home?</h2></div><p className="text-sm leading-6 text-[#667085]">No workshop queues or logistics. Choose a preferred time and have the work completed where it suits you.</p><p className="text-sm leading-6 text-[#667085]">A booking request is confirmed by our team before a worker travels to you.</p></div></section></>; }
