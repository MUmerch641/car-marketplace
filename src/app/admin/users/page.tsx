import{createClient}from"@/lib/supabase/server";export default async function Page({searchParams}:{searchParams:Promise<{q?:string;role?:string}>}){const{q,role}=await searchParams;const s=await createClient();let query=s.from("profiles").select("id,full_name,phone,role,created_at").order("created_at",{ascending:false}).limit(50);if(q)query=query.or(`full_name.ilike.%${q.replace(/[%_,]/g,"")}%,phone.ilike.%${q.replace(/[%_,]/g,"")}%`);if(role&&["customer","inspector","admin"].includes(role))query=query.eq("role",role as never);const{data}=await query;return <main className="p-5 lg:p-8"><h1 className="text-3xl font-bold">Users</h1><form className="mt-5 flex gap-2"><input name="q" defaultValue={q} placeholder="Name or phone" className="border p-2"/><select name="role" defaultValue={role}><option value="">All roles</option><option>customer</option><option>inspector</option><option>admin</option></select><button className="border px-3">Filter</button></form><div className="mt-5 space-y-2">{data?.map(x=><div className="border bg-white p-4" key={x.id}><b>{x.full_name||"Unnamed user"}</b><span className="ml-3 capitalize">{x.role}</span><p className="text-sm text-[#667085]">{x.phone||"No phone"} · Joined {new Date(x.created_at).toLocaleDateString("en-GB")}</p></div>)}</div></main>}
