import Link from"next/link";import{createClient}from"@/lib/supabase/server";import CountUp from"@/components/CountUp";import AnimatedContent from"@/components/AnimatedContent";export default async function Page(){const s=await createClient();const[{count:active},{count:cars},{count:bookings},{count:verification},{data:audit}]=await Promise.all([s.from("cars").select("id",{count:"exact",head:true}).eq("status","active"),s.from("cars").select("id",{count:"exact",head:true}).eq("status","pending_review"),s.from("service_bookings").select("id",{count:"exact",head:true}).eq("status","pending"),s.from("verification_requests").select("id",{count:"exact",head:true}).in("status",["pending","report_submitted"]),s.from("admin_audit_logs").select("action,entity_type,created_at").order("created_at",{ascending:false}).limit(8)]);const cards=[["Active cars",active],["Cars awaiting review",cars],["Bookings awaiting confirmation",bookings],["Verification actions",verification]];return <main className="p-5 lg:p-8"><h1 className="text-3xl font-bold">Operations overview</h1><AnimatedContent distance={14} duration={0.35} className="mt-6 grid gap-3 sm:grid-cols-2 xl:grid-cols-4">{cards.map(([l,n])=><div key={String(l)} className="border bg-white p-5"><p className="text-sm text-[#667085]">{l}</p><CountUp to={Number(n??0)} duration={0.7} className="mt-2 block text-3xl font-bold"/></div>)}</AnimatedContent><section className="mt-8"><h2 className="text-xl font-bold">Needs attention</h2><div className="mt-3 grid gap-3 md:grid-cols-3"><Link href="/admin/cars" className="border bg-white p-4 transition-colors hover:border-brand">{cars??0} cars awaiting moderation</Link><Link href="/admin/bookings?status=pending" className="border bg-white p-4 transition-colors hover:border-brand">{bookings??0} bookings awaiting confirmation</Link><Link href="/admin/verifications" className="border bg-white p-4 transition-colors hover:border-brand">{verification??0} verification actions</Link></div></section><section className="mt-8"><h2 className="text-xl font-bold">Recent admin activity</h2>{audit?.length?<div className="mt-3 space-y-2">{audit.map((x,i)=><div className="border bg-white p-3 text-sm" key={i}>{x.action.replaceAll("_"," ")} · {x.entity_type} · {new Date(x.created_at).toLocaleString("en-GB")}</div>)}</div>:<p className="mt-3 border border-dashed p-5 text-[#667085]">No recent admin activity.</p>}</section></main>}
