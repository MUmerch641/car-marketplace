"use server";import{revalidatePath}from"next/cache";import{requireRole}from"@/lib/auth/server";import{createClient}from"@/lib/supabase/server";export async function setStaffRoleAction(id:string,makeInspector:boolean){await requireRole("admin");const s=await createClient();const{error}=await s.rpc("set_staff_role",{p_user_id:id,p_make_inspector:makeInspector});if(error)return{error:"Unable to change staff role."};revalidatePath("/admin/staff");revalidatePath("/admin/users")}
