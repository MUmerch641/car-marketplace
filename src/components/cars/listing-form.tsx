"use client";
import { useActionState } from "react";
import { CarFront, MapPin, ReceiptText, SlidersHorizontal } from "lucide-react";
type State = { error?: string; success?: string }; const initial: State = {};
export function ListingForm({ action, car, isActive }: { action: (form: FormData) => Promise<State | void>; car?: Record<string, string | number | boolean | null>; isActive?: boolean }) { const [state, formAction, pending] = useActionState(async (_state: State, form: FormData) => (await action(form)) ?? {}, initial); const value = (name: string) => car?.[name] ?? ""; return <form action={formAction} className="space-y-6">
{isActive && <div className="rounded-xl border border-blue-200 bg-blue-50 p-4 text-sm text-blue-900 shadow-sm"><p className="font-semibold text-blue-950">This listing is live.</p><p className="mt-1">You can update price, mileage and seller-maintained details. Vehicle identity fields are locked while published.</p></div>}
<div className="grid gap-6 lg:grid-cols-2"><FormCard icon={<CarFront size={19} />} number="01" title="Vehicle identity" copy="The basics buyers search for."><div className="grid gap-4 sm:grid-cols-2"><Field name="make" label="Make" value={value("make")} required placeholder="e.g. BMW" disabled={isActive} /><Field name="model" label="Model" value={value("model")} required placeholder="e.g. 3 Series" disabled={isActive} /><Field name="variant" label="Variant" value={value("variant")} placeholder="e.g. M Sport" disabled={isActive} /><Field name="year" label="Year" value={value("year")} required type="number" placeholder="e.g. 2021" disabled={isActive} /></div></FormCard><FormCard icon={<SlidersHorizontal size={19} />} number="02" title="Price and specification" copy="Accurate details build buyer trust."><div className="grid gap-4 sm:grid-cols-2"><Field name="price" label="Price (£)" value={value("price")} required type="number" placeholder="e.g. 18500" /><Field name="mileage" label="Mileage" value={value("mileage")} required type="number" placeholder="e.g. 42000" /><Select name="fuel" label="Fuel type" value={value("fuel_type")} options={[["", "Choose fuel type"], ["petrol", "Petrol"], ["diesel", "Diesel"], ["hybrid", "Hybrid"], ["plug_in_hybrid", "Plug-in hybrid"], ["electric", "Electric"]]} disabled={isActive} /><Select name="transmission" label="Transmission" value={value("transmission")} options={[["", "Choose transmission"], ["manual", "Manual"], ["automatic", "Automatic"], ["semi_automatic", "Semi-automatic"]]} disabled={isActive} /><Field name="bodyType" label="Body type" value={value("body_type")} placeholder="e.g. SUV" disabled={isActive} /><Field name="engineSize" label="Engine size (L)" value={value("engine_size")} type="number" placeholder="e.g. 2.0" disabled={isActive} /><Field name="colour" label="Colour" value={value("colour")} placeholder="e.g. Black" disabled={isActive} /><Field name="registration" label="Registration" value={value("registration")} placeholder="e.g. AB12 CDE" disabled={isActive} /></div></FormCard><FormCard icon={<MapPin size={19} />} number="03" title="Location" copy="This helps local buyers find your car."><div className="grid gap-4 sm:grid-cols-2"><Field name="city" label="City" value={value("city")} required placeholder="e.g. London" /><Field name="postcode" label="Postcode" value={value("postcode")} required placeholder="e.g. SW1A 1AA" /></div></FormCard><FormCard icon={<ReceiptText size={19} />} number="04" title="Vehicle history" copy="Share the details buyers ask about."><div className="grid gap-4 sm:grid-cols-2"><Field name="motExpiry" label="MOT expiry" value={value("mot_expiry")} type="date" /><Select name="serviceHistory" label="Service history" value={value("service_history")} options={[["", "Not stated"], ["full", "Full service history"], ["part", "Partial service history"], ["none", "No service history"]]} /><Select name="ulezCompliant" label="ULEZ (seller stated)" value={value("ulez_compliant") === true ? "yes" : value("ulez_compliant") === false ? "no" : ""} options={[["", "Not stated"], ["yes", "Compliant"], ["no", "Not compliant"]]} /></div></FormCard></div><div className="rounded-2xl border border-slate-200 bg-slate-50/60 p-5 sm:p-6"><label htmlFor="description" className="text-base font-bold text-[#0b1f33]">Describe your car</label><p className="mt-1 text-sm text-slate-500">Mention condition, standout features and any known issues. Honest listings earn more trust.</p><textarea id="description" name="description" defaultValue={String(value("description") ?? "")} required minLength={20} rows={6} placeholder="Tell buyers what makes this car a good choice…" className="mt-4 w-full resize-y rounded-xl border border-slate-300 bg-white p-4 text-sm outline-none placeholder:text-slate-400 focus:border-[#d92d20] focus:ring-4 focus:ring-red-100" /></div>{state.error && <p className="rounded-xl bg-red-50 p-4 text-sm font-medium text-red-700" role="alert">{state.error}</p>}{state.success && <p className="rounded-xl bg-emerald-50 p-4 text-sm font-medium text-emerald-700" role="status">{state.success}</p>}<div className="flex flex-col-reverse gap-3 border-t border-slate-100 pt-6 sm:flex-row sm:items-center sm:justify-between"><p className="text-sm text-slate-500">You’ll add photos after saving your draft.</p><button type="submit" disabled={pending} className="rounded-xl bg-[#d92d20] px-6 py-3.5 text-sm font-bold text-white shadow-sm transition hover:bg-[#b42318] disabled:cursor-not-allowed disabled:opacity-60">{pending ? "Saving…" : car ? "Save changes" : "Save draft and add photos"}</button></div></form>; }
function FormCard({ icon, number, title, copy, children }: { icon: React.ReactNode; number: string; title: string; copy: string; children: React.ReactNode }) { return <section className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="flex gap-3"><span className="flex h-9 w-9 shrink-0 items-center justify-center rounded-xl bg-red-50 text-[#d92d20]">{icon}</span><div><p className="text-xs font-bold tracking-[.12em] text-slate-400">{number}</p><h3 className="font-bold text-[#0b1f33]">{title}</h3><p className="mt-0.5 text-sm text-slate-500">{copy}</p></div></div><div className="mt-5">{children}</div></section>; }
function Field({ name, label, value, required, type = "text", placeholder, disabled }: { name: string; label: string; value: string | number | boolean | null; required?: boolean; type?: string; placeholder?: string; disabled?: boolean }) { return <div><label htmlFor={name} className="mb-1.5 block text-sm font-bold text-slate-700 opacity-90">{label}{required && <span className="ml-1 text-[#d92d20]">*</span>}</label><input id={name} name={name} type={type} defaultValue={String(value ?? "")} required={required && !disabled} placeholder={placeholder} disabled={disabled} className="w-full rounded-xl border border-slate-300 px-3.5 py-2.5 text-sm outline-none placeholder:text-slate-400 focus:border-[#d92d20] focus:ring-4 focus:ring-red-100 disabled:bg-slate-50 disabled:text-slate-500 disabled:cursor-not-allowed" /></div>; }
function Select({ name, label, value, options, disabled }: { name: string; label: string; value: string | number | boolean | null; options: string[][]; disabled?: boolean }) { return <div><label htmlFor={name} className="mb-1.5 block text-sm font-bold text-slate-700 opacity-90">{label}</label><select id={name} name={name} defaultValue={String(value ?? "")} disabled={disabled} className="w-full rounded-xl border border-slate-300 bg-white px-3.5 py-2.5 text-sm outline-none focus:border-[#d92d20] focus:ring-4 focus:ring-red-100 disabled:bg-slate-50 disabled:text-slate-500 disabled:cursor-not-allowed">{options.map(([key, text]) => <option key={key} value={key}>{text}</option>)}</select></div>; }
