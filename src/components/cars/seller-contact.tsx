"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { createClient } from "@/lib/supabase/client";
export function SellerContact({ carId }: { carId: string }) { const router = useRouter(); const [contact, setContact] = useState<{ full_name: string | null; phone: string | null } | null>(null); const [error, setError] = useState<string | null>(null); const [pending, setPending] = useState(false); async function reveal() { setPending(true); setError(null); const supabase = createClient(); const { data: user } = await supabase.auth.getUser(); if (!user.user) { router.push(`/login?next=/cars/${carId}`); return; } const { data, error: rpcError } = await supabase.rpc("reveal_seller_contact", { p_car_id: carId }); if (rpcError || !data?.[0]) setError("Contact details are unavailable right now."); else setContact(data[0]); setPending(false); } if (contact) return <div className="rounded-md bg-[#F0FDF4] p-4 text-sm"><p className="font-bold text-[#166534]">{contact.full_name ?? "Seller"}</p>{contact.phone && <a href={`tel:${contact.phone}`} className="mt-1 block font-bold text-brand">{contact.phone}</a>}</div>; return <div><button onClick={reveal} disabled={pending} className="w-full rounded-md bg-brand px-5 py-3 text-sm font-bold text-white hover:bg-[#B42318] disabled:opacity-60">{pending ? "Loading contact…" : "Reveal Seller Contact"}</button>{error && <p role="alert" className="mt-2 text-sm text-[#B42318]">{error}</p>}</div>; }
