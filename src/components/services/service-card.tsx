import Link from "next/link";
import type { PublicService } from "@/lib/services/services";
export function ServiceCard({ service }: { service: PublicService }) { const duration = service.estimatedDurationMinutes ? `${service.estimatedDurationMinutes} mins` : "Mobile visit"; return <Link href={`/services/${service.slug}`} className="group border border-[#E4E7EC] bg-white transition-shadow hover:shadow-md focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-brand"><div className="border-b border-[#E4E7EC] bg-ink px-4 py-5"><p className="text-sm font-bold uppercase tracking-[.12em] text-[#F97066]">Mobile service</p><p className="mt-3 text-2xl font-bold text-white">From £{service.basePrice.toLocaleString("en-GB")}</p></div><div className="p-4"><h3 className="text-lg font-bold text-[#101828]">{service.name}</h3><p className="mt-2 line-clamp-3 text-sm leading-6 text-[#667085]">{service.shortDescription ?? service.description}</p><div className="mt-4 border-t border-[#EAECF0] pt-3 text-sm font-medium text-[#667085]">Estimated: {duration}</div></div></Link>; }
